import { View, Text } from "react-native";
import React from "react";

const ViewElement = () => {
  return (
    <View>
      <Text>View Element</Text>
      <Text>In React Native We Donot have div element</Text>
      <Text>Instead of using div element we use View element in React Native</Text>
    </View>
  );
};

export default ViewElement;

// In React Native we Donot have 'div' Element
// Instead of using 'div' Element we use 'View' Element in React Native